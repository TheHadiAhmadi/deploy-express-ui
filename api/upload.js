// deploy all functions

// run local server

//

function uploadSingleFile(filename) {}

function uplaod() {
  // upload all files inside functions

  const apiKey = process.env.MB_APIKEY;
  const apiUrl = process.env.API_URL;

  // fetch(`${url}/api/`);
}
